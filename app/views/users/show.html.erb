<h1>Hello <%= @user.name %></h1>

<h4>Here are a list of your past events</h4>
<ul>
  <% @user.events.each do |event| %>
    <li><%= event.date %> - <%= event.organization.name %> - <%= event.name %></li>
  <% end %>
</ul>
